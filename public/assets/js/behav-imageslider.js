//initialize image slider
$(function() {
	//configure slider settings
	$("#imageslider").responsiveSlides({
		auto: true,
		pager: true,
		nav: true,
		speed: 500,
		namespace: "centered-btns"
	});
});